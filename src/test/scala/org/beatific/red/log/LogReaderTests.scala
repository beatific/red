package org.beatific.red.log

import org.junit.Test
import scala.concurrent.ExecutionContext.Implicits.global

class LogReaderTests {
  
//  @Test
  def testReadline() {
//    val reader :LogReader = new LogReader("D:/source/scala/workspace/red/source/source.pc")
//    reader.read(str => println(str))
  }
}